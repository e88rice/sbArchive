<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.sbarchive.mapper.user.UserMapper">

    <insert id="registerUser">
        INSERT INTO user (userId, passwd, nickname, email, regDate)
        VALUES (#{userId}, #{passwd}, #{nickname}, #{email}, now())
    </insert>

    <select id="emailCheck" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE email = #{email}
    </select>

    <select id="userIdCheck" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE userId = #{userId}
    </select>

    <select id="loginCheck" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user WHERE userId = #{userId} AND passwd = #{passwd}
    </select>

    <select id="getUserInfo" parameterType="String" resultType="com.project.sbarchive.vo.user.UserVO">
        SELECT * FROM user WHERE userId = #{userId}
    </select>
</mapper>